import 'package:flutter/material.dart';

class TalimScreen extends StatelessWidget {
  const TalimScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Talim")),
    );
  }
}
