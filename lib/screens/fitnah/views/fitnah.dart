import 'package:flutter/material.dart';

class FitnahScreen extends StatelessWidget {
  const FitnahScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Fitnah")),
    );
  }
}
